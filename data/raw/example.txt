This example corpus outlines the end to end flow of building a lightweight knowledge base.
Source text is chunked, embedded with a sentence transformer, and stored inside a FAISS index.
The FastAPI service exposes ingestion and semantic query endpoints for downstream applications.
